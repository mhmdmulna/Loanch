# Loanch Frontend

React, TypeScript, Vite, Tailwind CSS, and ethers foundation for Loanch.

```bash
npm install
npm run dev
npm run lint
npm run build
```

Copy `.env.example` to `.env` before adding BOT Chain or contract configuration. Product pages, wallet flows, and contract integration are intentionally deferred to later phases.
